# Backend API base URL (required for Vercel/production builds).
# Local dev: backend on port 4000
VITE_API_URL=http://localhost:4000

# When you host the backend (Render, Railway, etc.), use its HTTPS URL instead:
# VITE_API_URL=https://your-api.example.com
